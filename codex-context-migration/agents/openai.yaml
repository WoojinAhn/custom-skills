interface:
  display_name: "Codex Context Migration"
  short_description: "Audit complex migrations beyond the official converter."
  default_prompt: "Use $codex-context-migration only for advanced multi-repo, workspace-copy, private-context, layering, posture, or authorization audit needs beyond $migrate-to-codex."

policy:
  allow_implicit_invocation: true
